# schema-validation 


# schema-validation for Kong apis

## Overview

The `schema-validation` for Kong Gateway provides schema validation functionalities for responses coming from backend.

## Installation

1. **Clone the repository:**

   ```bash
   git clone https://github.com/yourusername/schema-validation.git
   cd schema-validation

=========================================================================================
### 2. Install and Enable the Plugin

1. **Install the Plugin**

   If you have LuaRocks installed, navigate to the plugin directory and run:

   ```bash
   sudo luarocks make kong-plugin-schema-validation-0.1.0-1.rockspec










